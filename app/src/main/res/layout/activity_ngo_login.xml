<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black_bg"
    android:orientation="vertical"
    android:padding="24dp"
    android:gravity="center_horizontal">

    <ImageView
        android:src="@drawable/ic_phoenix_logo"
        android:layout_width="80dp"
        android:layout_height="80dp" />

    <TextView
        android:text="Welcome"
        android:textColor="@color/cyan_highlight"
        android:textSize="28sp"
        android:textStyle="bold"
        android:layout_marginTop="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <TextView
        android:text="NGO/NPO"
        android:textColor="@color/cyan_highlight"
        android:textSize="20sp"
        android:layout_marginBottom="24dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>


        <TextView
            android:text="NGO Login"
            android:textColor="@color/cyan_highlight"
            android:textSize="26sp"
            android:textStyle="bold"
            android:layout_marginBottom="24dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <EditText
            android:id="@+id/etNgoLoginEmail"
            android:hint="Email Address"
            android:inputType="textEmailAddress"
            style="@style/AuthEditText" />

        <EditText
            android:id="@+id/etNgoLoginPassword"
            android:hint="Password"
            android:inputType="textPassword"
            style="@style/AuthEditText" />

        <Button
            android:id="@+id/btnNgoLogin"
            android:text="Log In"
            style="@style/AuthButton"
            android:layout_marginTop="16dp"/>

        <TextView
            android:text="Don't have an account? Register"
            android:id="@+id/tvNgoRegister"
            android:textColor="@color/cyan_highlight"
            android:layout_marginTop="16dp"
            android:textSize="14sp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />
    </LinearLayout>
